name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Build Project
      run: |
        if [ -f pom.xml ]; then
          mvn clean install
        else
          echo "No pom.xml found at repository root. Skipping Maven build."
        fi

    - name: Run Tests
      run: |
        if [ -f pom.xml ]; then
          mvn test
        else
          echo "No pom.xml found at repository root. Skipping Maven tests."
        fi
